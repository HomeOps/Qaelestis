- sensor:
  - name: "Oscar Phone Number"
    state: !secret sms_oscar
- sensor:
  - name: "Hazel Phone Number"
    state: !secret sms_hazel
- binary_sensor:
  - name: "Main Tank Water Low"
    state: "{{ states('sensor.main_tank_water_level') | float(default=0) <= 50 }}"
